'use strict';

const reactComponents = require('..');
const assert = require('assert').strict;

assert.strictEqual(reactComponents(), 'Hello from reactComponents');
console.info("reactComponents tests passed");
